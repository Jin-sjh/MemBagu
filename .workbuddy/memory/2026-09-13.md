# 2026-09-13

## 八股沉淀：vLLM 推理加速原理

- 用 `bagu-organizer` skill（Mode A，从对话沉淀）生成新条目
  `src/data/AI/AI_vLLM 推理加速原理.md`，未复用/覆盖已有文件（此前无 vLLM 相关条目）。
- frontmatter：`category: AI` / `topic: vLLM 推理加速原理` / `type: bagu` / `difficulty: hard`
  / `tags: [AI, vLLM, 推理加速, PagedAttention, Continuous Batching, 大模型]`。
- 共 8 条问答：因果链总览、decode memory-bound 量化地基（访存/计算 ≈ 76×）、
  PagedAttention 三类碎片辨析、算子级 vs 系统级代价、Continuous Batching vs 经典
  dynamic batching（Orca / selective batching）、两项机制互为前提、Chunked Prefill 的
  TTFT–TPOT 权衡、吞吐数据引用口径（2–4× 论文 vs 23× Anyscale）。
- 6 条衍生问题标注「待补充」：Prefix Caching 块级哈希、CUDA Graph / piecewise、
  Speculative Decoding、V0 vs V1 调度差异、Copy-on-Write 前缀共享、PD 分离。

### 发现的问题（待收敛）

- `src/data/AI/AI_KV Cache 缓存机制.md` 等历史条目使用 `## 问题 N：xxx` + `### 问题难点分析`
  的写法，与 `bagu-organizer/references/format-spec.md` 规定的 `## 【问题】` / `## 【回答】`
  及 `## 【难点分析】` 增强块不一致。`###` 行会被 `src/utils/parser.js` 计入上一个块的正文，
  导致难点分析混入 answer。新条目已按规范格式产出，历史文件待批量收敛。

## 八股沉淀：推理吞吐瓶颈定位（承接 vLLM 条目）

- 新增 `src/data/AI/AI_推理吞吐瓶颈定位.md`（category: AI / topic: 推理吞吐瓶颈定位 /
  type: bagu / difficulty: hard），Mode A 从对话沉淀。
- 来源：Jin 给出的面试题「显存还有很多，为什么吞吐上不去了？」及其实习沉淀的推导式回答。
- 1 条问答，核心是三步定位法：profiler 判 compute-bound / memory-bandwidth-bound →
  GPU 未打满则查调度层（effective batch、vLLM scheduler、GPU timeline kernel gap）→
  仍 idle 则查 CPU 侧（scheduler、tokenization/detokenization、Python 与 kernel launch
  overhead）。另附【难点分析】【考察点】【衍生问题】三个独立增强块。
- 未与 `AI_vLLM 推理加速原理.md` 合并（那条讲「vLLM 为什么快」的机制因果链，本条讲
  「瓶颈在哪」的定位路径，同域互补），仅在衍生问题中双向往引。
- 格式与同日产出的 `AI_vLLM 推理加速原理.md` 保持一致（`## 【问题】`/`## 【回答】` +
  `## 【xxx】` 独立增强块）。

## 面经收录：腾讯 IEG 全栈开发岗（用 interview-extractor skill）

- Jin 丢进一张面经截图（微博/小红书风格帖子，标签 #腾讯ieg #全栈开发 #前端 #后端 #测试开发），
  用 `interview-extractor` skill 处理，落盘新建 `src/data/面经/面经_腾讯IEG全栈开发岗.md`
  （此前 `src/data/面经/` 只有字节那一条，腾讯为新增、无重复需合并）。
- frontmatter：`category: 面经` / `topic: 腾讯IEG全栈开发岗` / `company: 腾讯` /
  `position: IEG 全栈开发岗（AI＋游戏方向）` / `rounds: [一面, 二面, 三面, HR面]` /
  `difficulty: hard`。
- 原文是**纯问题清单、零答案**，21 个【问题】/【回答】块均为回答建议（开头统一标注）。
  一面 5 题可见 + 二面 13 题 + 三面/HR 面说明各 1 条；**一面第 1 题在截图顶部被截断**，
  已标注「内容未提及」，待 Jin 补图后合并。
- 用 `src/utils/parser.js` 实测验证解析：21 条问答全部成对，`rounds` 数组正确解析，
  每个 answer 均以 `>` 回答建议标注开头（解析器把 `## 【衍生问题】`/`## 【考察点】`
  正确排除在 answer 之外）。
- 面经结构判断：19 题中纯技术题仅约 6 道，其余为学习迁移、战略判断、意愿匹配与项目拷打；
  二面 10/11、12/13 各为一题两问，是**稳定性筛选信号**；三面 5 分钟 vs HR 面 1 小时，
  HR 面才是软性淘汰环节。Jin 的最大短板是**零游戏业务/引擎经验**（二面 6/7/8/12/13）。

## 八股沉淀：v-memo 与 computed 对比

- 新增 `src/data/frontend/Vue_v-memo 与 computed 对比.md`（category: Vue / topic:
  v-memo 与 computed 对比 / type: bagu / difficulty: hard / tags: [Vue, 性能优化, 响应式]）。
- 由 Jin 提供的讲解稿蒸馏为 3 条问答：核心区别（缓存数据值 vs 缓存 VNode 子树）、
  v-memo 踩坑点（不能当变量取值、漏依赖隐性 bug、浅比较、不要滥用）、
  computed 为何能减少子组件渲染却不能阻止父组件自身重渲染。
- 附【考察点】与 3 条待补充衍生问题：v-memo vs v-once、v-memo 配合 v-for 的写法、
  编译时优化（静态提升 / patchFlag / 事件缓存）与 v-memo 的生效阶段差异。
- scan 确认此前无 v-memo 相关条目，为新建而非合并。

