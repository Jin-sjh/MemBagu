# 2026-09-21

## 桌面《秋招面试/wiki》整体沉淀入 MemBagu

- 来源：`C:\Users\ThinkPad\Desktop\秋招面试\wiki`，150 个 md / 658 KB，**109 个知识页**（19 个领域）
  （讲解型结构：一句话理解 → 核心机制 → 场景 → 成本优化表 → 替代方案 → 验证 → 30秒/1-2分钟回答
  → 高频追问 → 高频误区 → 来源）。用 bagu-organizer 的 Mode B 蒸馏。
- 做法：先建「源文件 → 目标文件」1:1 映射表（由主代理定表，避免多个子代理抢同一个目标文件），
  再按领域派 15 个子代理分两波并行蒸馏 + 落盘；每波 7~8 个，每个子代理 4~13 个源文件。
- 结果（**实测，用 git show HEAD 对比得出，比子代理自报更准**）：
  109 个源页 → 109 个目标条目，覆盖率 100%，无遗漏。
  **新建 66 个条目 / 397 条问答 + 合并 43 个条目（261 → 453 条）/ 新增 192 条 = 本次新增 589 条问答。**
  合并文件最小增量 +3，没有空写入。
  category 分布（frontend 107 + cs-basics 2）：JavaScript / TypeScript / Vue / React / HTTP / TCP / HTTPS / CSS / HTML /
  浏览器 / 工程化 / Webpack / Vite / Git / 性能 / 渲染策略 / 前端架构 / NestJS（13 个）/ AI前端。
  cs-basics 新增 `网络安全_CSP与点击劫持.md`，合并扩充 `网络安全_XSS 攻击详解.md`。
- 有意跳过（已在汇报中向 Jin 说明）：`_reviews/` 36 份审查记录、
  `README.md` / `00-知识地图.md` / `10-学习状态.md` 元文件、
  `09-面试问题索引.md` / `14-CSS/00-CSS题目索引.md` 两份纯问题清单（无答案，入库会污染条目的空答案）。
- 校验：全库 609 条目，今日 66 个新文件 category/topic 与文件名 100% 一致、问答块全部配对、
  无过短答案；7 个不一致条目均为 AI 库历史遗留，与本次无关。
- 已知遗留：`JavaScript_闭包.md` 依然堆积多条重复「什么是闭包？」追问（前次已发现），
  本次仅追加未去重，待 Jin 授权后做一轮合并。

- 教训：**子代理自报的条数会偏少**（本次自报合计约 380，实测 589，低估约 35%），
  批量落盘后必须用 `git show HEAD:路径` 对比统计真实增量，别信汇报加总。
- **PowerShell 工具输出被吞**：两次调用都是 exit 0 但无 stdout，别指望它做探查；
  改用 Glob / Read / 绝对路径 python 脚本。
- **本机 git 可用，但不在 PATH 里**（系统 PATH 未包含 Git）：绝对路径是
  `C:\Users\ThinkPad\.workbuddy\binaries\PortableGit\versions\1.2.0\cmd\git.exe`。
  用它做文件历史对比很方便（`git -C <repo> show HEAD:<relpath>`），中文路径也能正常取回。
- **Bash 工具的 PATH 是坏的**：shim 脚本报 `dirname: command not found`，
  `ls` / `head` / `tail` / `find` 全部 127，只有 `echo` 等内建命令可用。
  需要执行系统操作时用绝对路径：`C:/Users/ThinkPad/.workbuddy/binaries/python/versions/3.13.12/python.exe`
  或 node 绝对路径；`-c` 内联里的 `\s` 仍会被吞（旧坑，依旧成立），改写成脚本文件更稳。

## 笔试速记版讨论沉淀（5 个判断题陷阱）

Jin 发来一段「笔试速记版」讨论（5 个主题），走 bagu-organizer Mode A 沉淀，新建 5 个条目：

| 文件 | library | category |
| --- | --- | --- |
| `MySQL_索引类型与聚簇索引.md` | cs-basics | MySQL（新 category） |
| `Java_方法重写与重载.md` | cs-basics | Java（新 category） |
| `网络安全_无线网络安全协议.md` | cs-basics | 网络安全（复用） |
| `设计模式_装饰器模式.md` | cs-basics | 设计模式（新 category） |
| `大模型_多头注意力 QKV 维度.md` | AI | 大模型（复用） |

共 16 组问答。每个文件都补了「【考察点】陷阱清单 + 【口诀】 + 【衍生问题】待补充」三件套，
判断题型可以直接从【考察点】背。
分类决策：索引虽可归已有的 `SQL`，但存储引擎层面另起 `MySQL` 更利于后续沉淀事务/B+树/锁，
与 cs-basics 里 `操作系统` / `网络安全` 的粒度并列。

- 教训补充：**Glob 带 `path` 参数时子目录 pattern 会返回空**（`cs-basics/*.md` 无结果），
  改成不带 path 的 `**/cs-basics/**` 才正常。列子目录优先用无 path 的全局 pattern。
