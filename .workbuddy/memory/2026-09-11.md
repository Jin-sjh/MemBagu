# 2026-09-11 工作日志

## 用 bagu-organizer 沉淀「Vue 核心知识体系（大厂秋招方向）」长文

**输入来源**：Jin 粘贴的 `# Vue 核心知识体系（大厂秋招方向）` 全文（Mode B 讲解型资料，需蒸馏）。

**逐段比对 24 个已有 Vue 条目后的覆盖度结论**：资料约 80% 内容已被现有条目覆盖
（响应式系统原理 / ref 与 reactive / 虚拟 DOM 原理 / 静态提升 / 事件缓存 /
Composition_API / 组件通信方案 / 核心区别 / 双向绑定原理），真正缺口只有 3 处。

**落盘动作**：

| 操作 | 文件 | 内容 |
| --- | --- | --- |
| 新建 | `src/data/frontend/Vue_patchFlag.md` | patchFlag「少比较」定位、`createVNode(..., PatchFlags.TEXT)` 编译产物、与静态提升/事件缓存/Block Tree 的四项分工表 |
| 合并 | `Vue_Vue3 diff 性能优化.md` | +「Vue3 为什么比 Vue2 快（综合）」（Proxy 降本 / 编译优化 / Block Tree / 静态提升）+ 4 条【考察点】 |
| 合并 | `Vue_什么是 Vue.js.md` | +「面试怎么讲 Vue 本质」（低分=MVVM 标签，高分=响应式驱动的组件渲染系统）+ 完整链路 |
| 合并 | `Vue_响应式系统原理.md` | +「为什么改变量页面会变」直觉层费曼问答（原有条目直接进源码层，缺一面开场） |
| 修订 | `Vue_静态提升.md` / `Vue_事件缓存.md` | 把已过期的「事件缓存/patchFlag 待补充」改为指向对应专文 |

**未落盘（资料本身没给答案，禁止编造）**：nextTick 原理、Block Tree 细节、SSR、
KeepAlive/Teleport/Suspense、Vue Router 源码 —— 已在相关条目记为【衍生问题】待补充。

## 顺手修正 skill 文档

`bagu-organizer/references/format-spec.md`：原文写「library 固定为四个之一」，但实际仓库存在
遗留顶层目录 `强化学习`（1 文件）与 `面经`（1 文件），且 `强化学习` 同时是 AI 下的 category
（14 文件）。已补充说明：新建条目不得落入遗留目录，并提示后续收敛。**遗留目录尚未合并，待 Jin 决定。**

## 重复投喂纠错（晚间）

Jin 再次粘贴「为什么修改 Vue 数据页面会自动更新」主题长文（直觉科普版，含 track/trigger/effect 三步演示）。
**初判失误**：第一次 Glob `src/data/frontend/*.md` 用了 `limit: 100` 被截断，没察觉结果不全，
误判 `响应式系统` topic 为空，遂新建了 `src/data/frontend/Vue_响应式系统.md`。
核对本次会话前段的当日日志 + 完整文件列表后确认：**该主题已被 100% 覆盖，新建文件属 skill 明令禁止的副本文件，已删除。**

| 材料内容 | 已有覆盖位置 |
| --- | --- |
| 为什么修改变量页面就变（直觉层） | `Vue_响应式系统原理.md` 第 3 条问答 |
| track / trigger / effect 闭环 | `Vue_响应式系统原理.md`（源码级，含 targetMap 三层结构 + activeEffect） |
| defineProperty vs Proxy 及各自缺点 | 同上「必背对比表」+【口诀】 |
| ref 为什么必须 `.value` | `Vue_ref 与 reactive.md` 第 3 / 第 7 条（RefImpl 源码级） |
| 材料末尾的自测三问 | 同上【考察点】四条 |

**教训（下次直接照做）：**

1. 分类查重前**先读当日 memory 日志** —— 同日重复投喂同一主题的概率很高。
2. Glob 返回值若接近 `limit` 数量，必须放大 limit 复检，**不可在截断结果上判断「topic 不存在」**。
3. 查重须覆盖同义 topic（`响应式系统` / `响应式系统原理` / `ref 与 reactive`），不能只做精确名匹配。
