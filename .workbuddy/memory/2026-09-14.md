# 2026-09-14

## 八股沉淀：Vue 3 生命周期钩子注册时机
- 用 bagu-organizer (Mode A) 合并进 `src/data/frontend/Vue_Composition_API.md`（未新建副本文件），新增 1 条问答 + 【衍生问题】3 条待补充，frontmatter tags 追加「生命周期钩子, setup」。
- 沉淀时做了两处超出原始讨论的补充/修正：① `<script setup>` 顶层 await 由 `withAsyncContext` 恢复实例，await 后注册仍有效（普通 setTimeout/.then 仍失效）；② 与 React Hooks 的差异是「调用顺序 vs 实例上下文」，Vue 允许在 if/for 里调用。
- 环境备忘：本工作区 Bash 的 shim 环境缺 `dirname`/`head`，`python xxx.py | head` 会 127；改用绝对路径 `C:/Users/ThinkPad/.workbuddy/binaries/python/versions/3.13.12/python.exe` 直接跑脚本即可。

## 八股沉淀：Vue onUpdated 修改响应式状态
- 新建 `src/data/frontend/Vue_onUpdated 中修改响应式状态.md`（category=Vue, type=bagu, difficulty=medium），2 条问答（为什么不建议改状态 / onUpdated 适用边界与替代方案）+ 【考察点】+【衍生问题】（三钩子执行时机对比待补充）。
- 已向 Jin 提出是否把 onMounted / onBeforeUpdate / onUpdated 执行时机对比补成正式问答，待其选择。

## 规则确立：八股题一律走 bagu-organizer 沉淀
- Jin 明确：「当前项目记住我发这种题就用 bagu skill 处理沉淀」。已写入 `.workbuddy/memory/MEMORY.md`。
- 含义：对话里给简答（可背的版本）+ 调用 bagu-organizer 落盘，两件事一起做，不再只答不存。

## 八股沉淀：JavaScript 闭包（合并）
- 合并进已有 `src/data/frontend/JavaScript_闭包.md`（未新建副本）：tags 追加 `垃圾回收, V8, React`，created 保持 2026-07-24。
- 新增 5 条问答：外层变量为何不被 GC（`[[Environment]]` + 可达性）、`for(var i)` 经典坑（捕获绑定而非快照）、闭包是否必然泄漏（V8 context specialization，eval/断点时退化为保留整个 Context）、实现细节坑（`num++` 返回旧值 / 多闭包共享变量 / 持有 DOM）、React stale closure；外加【考察点】【衍生问题】（3 条待补充）。
- 该文件历史累积多条重复「什么是闭包？」问答，已记为待办，未擅自删除。
- 分类/查重扫描改用 Glob `src/data/**/*.md`（Bash 管道不可用时的替代方案）。

## 八股沉淀：React 渲染 10 万行大表格优化（Mode B 资料抽取）
- 新建 `src/data/frontend/React_10万行大表格渲染优化.md`（category=React, difficulty=hard）。**这是 frontend 下第一个 React 条目**，此前无 React category。
- 从粘贴资料蒸馏出 9 条问答：核心思路 / 虚拟滚动原理 / React 层三要点（memo·key·行高）/ 数据层（ref + useMemo）/ Fiber 调度（拆组件·Context·useTransition）/ DOM 样式层（固定表头·节流）/ 额外优化（分页·props·WebWorker）/ 选型与架构 / 5 个常见坑点；外加【考察点】+ 4 条【衍生问题】待补充。
- 原文的表格、代码块、架构树、⚠️ 内联提示全部按原样保留，关键结论已加粗（供解析器统计 keyPointsCount）。
- 已识别潜在重叠：`frontend_前端性能优化.md` 有一条**浅层**「虚拟列表实现原理」问答（generic 版），未合并（粒度不同），已向 Jin 提出是否互相引用/收敛。
