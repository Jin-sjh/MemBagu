# 问题 1：哪些操作会造成强引用，如何避免？

【问题】哪些操作会造成强引用，如何避免？

【回答】强引用是默认的引用关系，只要强引用存在，GC 就不会回收对象，常见场景包括：全局变量、未销毁的定时器、未解绑的事件监听、闭包、长列表缓存、第三方实例未销毁等。

避免方案：① 避免全局变量，用块级作用域；② 组件卸载时清除定时器、解绑事件；③ 用 WeakMap/WeakSet 替代普通对象做缓存；④ 长列表用虚拟列表减少 DOM 数量；⑤ 第三方实例调用 dispose 销毁。
