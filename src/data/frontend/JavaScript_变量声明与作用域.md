# 变量声明与作用域

## 【问题】
解释 `var`、`let` 和 `const` 的区别

## 【回答】

| 特性 | var | let | const |
|------|-----|-----|-------|
| 作用域 | 函数作用域 | 块级作用域 | 块级作用域 |
| 变量提升 | 是 | 否（暂时性死区） | 否（暂时性死区） |
| 重复声明 | 允许 | 不允许 | 不允许 |
| 初始值 | 可不初始化 | 可不初始化 | 必须初始化 |
| 重新赋值 | 可以 | 可以 | 不可以 |

## 【问题】
什么是作用域链？

## 【回答】

在 JS 中，变量查找是按照**当前作用域** → **外层作用域** → **全局作用域**的顺序一层层找，这个层级关系就是**作用域链**。作用域链是在**函数定义时就确定的**，和执行位置无关。
