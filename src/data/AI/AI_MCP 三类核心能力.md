---
category: AI
topic: MCP 三类核心能力
type: bagu
tags: [AI, MCP, Function Calling]
difficulty: medium
created: 2026-07-24
---
# MCP 三类核心能力

## 【问题】
MCP（Model Context Protocol）提供哪三类核心能力？各自的用途是什么？

## 【回答】
MCP 是 Anthropic 提出的开放协议，为 LLM 应用提供标准化的外部上下文接入方式。它定义了三种原语（Primitives）：

### 1. Tools（工具）— 可执行的函数
- **本质**：让 LLM 能够调用外部函数执行操作，如查询数据库、发送消息、调用 API。
- **特点**：由模型控制执行时机（模型主动选择调用），服务器暴露工具列表，模型根据用户意图决定调用哪个工具。
- **典型场景**：数据库 CRUD、发邮件、创建文件、调用第三方 API。

### 2. Resources（资源）— 可读取的数据
- **本质**：暴露结构化的只读数据给模型，类似 GET 请求。模型可以像访问文件系统一样读取这些数据。
- **特点**：由客户端控制读取时机（客户端主动拉取），通过 URI 标识资源，支持文本和二进制内容。
- **典型场景**：读取文件内容、获取文档片段、查询知识库条目、API 响应数据。

### 3. Prompts（提示模板）— 可复用的提示
- **本质**：预定义的提示模板，帮助用户和模型快速开始特定任务。
- **特点**：服务器定义一组可复用的 prompt 模板，支持参数化，用户通过客户端选择和填写参数后使用。
- **典型场景**：代码审查模板、会议纪要模板、标准化问答流程。

### Tools vs Resources 的核心区分
| 维度 | Tools | Resources |
|------|-------|-----------|
| 操作性质 | 执行操作（写） | 读取数据（读） |
| 控制方 | 模型驱动 | 客户端驱动 |
| 有副作用 | 是 | 否 |
| 类比 | REST POST/PUT | REST GET |

MCP 通过这三类原语将 LLM 从封闭系统变为可连接外部世界的开放代理，是构建 AI Agent 的基础协议。

## 【问题】
MCP 主要解决什么痛点？企业为什么愿意接 MCP 而不是每个业务线自己写 Function？

## 【回答】
MCP 解决的是**工具集成碎片化、重复建设、难以跨产品复用**的痛点：在此之前每个产品各写一套插件/工具适配。MCP 提供标准边界（Server）与传输（stdio / HTTP / SSE），使工具像外设一样「即插即用」——一次实现 Server，多个客户端（Claude Desktop、IDE、自研 Agent）可复用。

企业价值：降低重复建设、统一安全与观测、换模型不换工具链、利于平台组与业务组分工。治理上工具集中在 MCP Server，便于审计、版本与权限；敏感系统只对内网 Server 开放，模型不直连数据库。

## 【问题】
MCP 与 Function Calling 是替代关系吗？

## 【回答】
不是替代，是**互补**。Function Calling 解决「**模型怎么表达调用**」（输出调用指令）；MCP 解决「**工具能力怎么暴露与连接**」（系统级协议：能力发现、资源读取、工具调用、提示模板）。二者常一起出现：模型侧用 FC，工具侧来自 MCP；Host 常把 MCP Server 的工具列表映射为 FC 的 `tools` 定义再交给模型。

MCP 与 OpenAPI 网关不同：OpenAPI 面向通用 HTTP 客户端，MCP 面向 AI Host 与模型工具循环，带会话、资源、提示等 AI 原生语义。
